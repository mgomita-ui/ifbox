# 貢献のしかた

IFBOX は「リナックスのように作り、協同組合のように分ける」を試しています。貢献は誰でもできます。取り込むかどうかは、責任を持つ人が決めます。

## 席

このリポジトリ自体が IFBOX の案件第0号です。貢献者は席に座ります。

| 席 | 誰が | やること |
|---|---|---|
| 発案 | 創業メンバー | 目的と達成条件を決める。取り込みを決める（マージ権限） |
| 作る | 貢献者 | Issue を取って PR を出す。小さく、1つの変更に1つのPR |
| 売る | 貢献者 | 使い方の説明、導入の手伝い |
| 検収 | 発案者が指名した人 | 受入条件（`proto/test/scenario_round*.js`）が通ることを確認 |

## 進め方

1. Issue を立てる（「もし、〜だったら」で始めてもよい）。
2. 発案者が席と受入条件を置く。
3. PR を出す。差分の説明に「前の版からの変更」を書く。
4. 検収席がヘッドレスの受入条件を通す。全部通ったら発案者が取り込む。
5. 貢献は台帳に残る。再分配は「貢献への報酬」として扱う（持分の配当ではない）。

## 受入条件の回し方

```bash
cd proto/test
python -m http.server 8769 --bind 127.0.0.1
# 別の端末で
chrome --headless=new --disable-gpu --virtual-time-budget=30000 --dump-dom http://127.0.0.1:8769/run8.html
```

`<pre id="RESULT">` に JSON が出ます。`errors` が空で、各項目が期待通りなら合格です。

## 書かないもの

- 実在の企業名・個人名・送金情報。サンプルは「例：」付きの仮名で。
- 共有DBの中身（履行の記録）。コードだけを扱う。
- 席の仕組みの細部を、このリポジトリの外（ブログ・SNS）に書かない（特許出願前）。

## 決めごと

- 段階は人の確定でしか上がらない。AI は準備と提示だけ。この原則を崩す変更は取り込まない。
- IFBOX の取り分を配分AIに決めさせない。
- 保存済みの版は上書きしない。修正は新しい版。
